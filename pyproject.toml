[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "opc"
version = "0.1.0"
description = "One-Person Company — Autonomous AI Agent collaboration system with natural language interface"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}

dependencies = [
    "aiohttp>=3.9.0",
    "typer>=0.9.0",
    "rich>=13.0",
    "prompt-toolkit>=3.0",
    "litellm==1.82.1",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "pyyaml>=6.0",
    "aiosqlite>=0.19.0",
    "chromadb>=0.4.0",
    "httpx>=0.27.0",
    "loguru>=0.7.0",
    "playwright>=1.40",
    "mcp>=1.0.0",
    "anyio>=4.0",
    "python-docx>=1.1.0",
    "openpyxl>=3.1.0",
    "python-pptx>=0.6.21",
]

[project.optional-dependencies]
nu = [
    "nu-llm-routing-lib==0.4.0; python_version >= '3.11'",
    "nu-resource-gen-lib==0.2.2; python_version >= '3.11'",
]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-timeout>=2.4",
    "ruff>=0.15",
]
# Backwards-compatible alias: browser tools are now native and the dependency
# is installed with the base package.
browser = []
# Optional legacy CLI board/TUI inspector; not installed by default.
cli-board = ["textual>=8.1.1"]
channels-telegram = ["python-telegram-bot>=21.0"]
channels-whatsapp = ["websockets>=12.0"]
channels-discord = ["discord.py>=2.3"]
channels-feishu = ["lark-oapi>=1.4.0"]
channels-mochat = ["python-socketio[asyncio_client]>=5.11.0"]
channels-dingtalk = ["dingtalk-stream>=0.21.0"]
channels-email = []
channels-slack = ["slack-sdk>=3.27.0"]
channels-qq = ["qq-botpy>=1.2.1"]
channels-matrix = ["matrix-nio>=0.24.0"]
channels-bridges = [
    "websockets>=12.0",
    "python-socketio[asyncio_client]>=5.11.0",
]
channels-all = [
    "python-telegram-bot>=21.0",
    "websockets>=12.0",
    "discord.py>=2.3",
    "lark-oapi>=1.4.0",
    "python-socketio[asyncio_client]>=5.11.0",
    "dingtalk-stream>=0.21.0",
    "slack-sdk>=3.27.0",
    "qq-botpy>=1.2.1",
    "matrix-nio>=0.24.0",
]
all = [
    "python-telegram-bot>=21.0",
    "websockets>=12.0",
    "discord.py>=2.3",
    "lark-oapi>=1.4.0",
    "python-socketio[asyncio_client]>=5.11.0",
    "dingtalk-stream>=0.21.0",
    "slack-sdk>=3.27.0",
    "qq-botpy>=1.2.1",
    "matrix-nio>=0.24.0",
]

[project.scripts]
opc = "opc.cli.app:main"

[tool.uv.sources]
nu-llm-routing-lib = { path = "../nu-llm-routing-lib", editable = true }
nu-resource-gen-lib = { path = "../nu-resource-gen-lib", editable = true }

[tool.pytest.ini_options]
testpaths = [
    "tests",
    "opc/plugins/office_ui/tests",
]

[tool.hatch.build.targets.wheel]
packages = ["opc"]

[tool.hatch.build.targets.wheel.force-include]
"config" = "opc/config_templates"
"skills/core" = "opc/skills_assets/core"

[tool.hatch.build.targets.sdist]
include = [
    "opc",
    "config",
    "skills/core",
    "docs",
    "scripts",
    "README.md",
    "LICENSE",
    "pyproject.toml",
]
